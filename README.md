# core-api-gateway

Central API gateway for the CROW platform. Routes and authenticates all external requests.

## Setup

```bash
bun install
```

## Dev

```bash
bun run dev
```

Runs on `localhost:8000`.
